# Japan moto — Інструкція користувача

Проста програма для заповнення актів МВС, автоматичного створення договору та видаткової накладної.

---

## Що вміє програма

- Читає ваш акт 6055 (файл XLS, який ви скачали з сайту МВС)
- Дозволяє вписати три поля: **номер акта**, **дата народження клієнта** і **номер транзиту**
- Записує ці дані прямо в ваш акт — нічого не копіює зайвого
- Автоматично створює **Договір** і **Видаткову** в ту саму папку, де лежить акт
- Генерує текстовий файл з номерами транзиту (у папку `Транзит` поруч із програмою)

---

## Перший запуск

1. Розпакуйте архів у будь-яку зручну папку (наприклад `C:\Program Files\JapanMoto`)
2. Запустіть `JapanMoto.exe` — або перетягніть ярлик `Japan moto.lnk` на робочий стіл
3. Вікно одразу запитає де лежить ваш акт МВС — натисніть **📂 Огляд** і знайдіть XLS-файл
4. Натисніть **Далі →**
5. У трьох полях введіть потрібні дані або натисніть **📋 Вставити**, якщо скопіювали дані з буфера
6. Як тільки всі три поля заповнені — програма запитає: **Генерувати документи зараз?** — тисніть **Так**
7. Документи збережуться там, де лежить акт. Відкриється папка з результатом.

---

## Кнопка «Вставити» (буфер обміну)

Виділіть у браузері або документі рядок такого формату:
```
№434232/25/222222 від 15 березня 2026 року 12.02.1991 АВ31343ВА3
```
Скопіюйте його (Ctrl+C), потім натисніть **📋 Вставити** — програма сама розкладе все по полях.

- Рядок вигляду `№NNN/NN/NNNNNN від ДД місяць РРРР` → йде в **Номер акта**
- Дата `ДД.ММ.РРРР` → в **Дата народження**
- Транзитний номер (два кириличних + цифри + два кириличних) → в **Номер транзиту**

---

## Де зберігаються файли

| Що | Де |
|---|---|
| Договір №XXXXX.doc | В папці де лежить акт |
| Видаткова №XXXXX.xls | В папці де лежить акт |
| Номери транзиту №XXXXX.txt | Папка `Транзит` поруч із програмою |
| Налаштування (jm_config.json) | Поруч із програмою |
| Логи | Папка `logs` поруч із програмою |

> **Важливо:** у папці з актом залишиться лише 3 файли — оригінальний акт (змінений), договір і видаткова. Жодних системних файлів там не буде.

---

## Шестерня (⚙) — повна форма

Якщо потрібно заповнити більше полів (технічні дані, ціна тощо) — натисніть **⚙** у правому верхньому куті другого кроку. Відкриється повна форма з усіма полями.

---

## Налаштування

Натисніть **⚙ Налаштування** в головному вікні.

| Параметр | Що робить |
|---|---|
| **Тема** | Світла або темна (або авто — за часом доби) |
| **Масштаб інтерфейсу** | Змінює розмір шрифту і вікон. Набирає чинності після перезапуску |
| **Шаблон акту** | Шлях до файлу-шаблону акту (6055.xls) |
| **Шаблон договору** | Шлях до файлу договору (.doc/.docx) |
| **Шаблон видаткової** | Шлях до файлу видаткової (.xls) |
| **Папка збереження** | Куди зберігати готові документи (за замовчуванням — туди ж де акт) |
| **Генерувати новий акт (копія)** | Якщо увімкнено — поруч з актом з'явиться копія з вашими даними. Якщо вимкнено (за замовчуванням) — програма просто записує дані прямо в обраний акт |
| **Запитувати папку при генерації** | Перед збереженням програма запитає куди покласти файли |
| **Підтека для кожного клієнта** | Створює окрему папку для кожного клієнта |
| **Зберегти форматування клітинок акту** | Якщо увімкнено — текст у клітинках залишиться по центру як у шаблоні |
| **Формат договору** | doc або docx |
| **Відкривати після збереження** | Автоматично відкриває збережені файли |
| **Word COM** | Використовує Microsoft Word для генерації (лише Windows + MS Office). Якщо Word не встановлено — вимкніть |

---

## Де взяти шаблони

Шаблони (6055.xls, vidatkova.xls, DOGOVIR_6055_template.doc) — це **порожні форми**, які лежать поруч із програмою. Ви можете замінити їх своїми:

- `6055.xls` — бланк акту МВС (скачайте чистий бланк з сайту МВС)
- `vidatkova.xls` — ваша видаткова накладна (шаблон)
- `DOGOVIR_6055_template.doc` — ваш шаблон договору з плейсхолдерами

Плейсхолдери у договорі — це теги виду `{{ПІБ}}`, `{{A3}}` тощо. Програма замінює їх реальними даними.

---

## Якщо щось пішло не так

- Перевірте папку `logs\japan_moto.log` поруч із програмою — там є детальний журнал
- Переконайтесь що шлях до шаблонів вказаний правильно в налаштуваннях
- Якщо не створюється договір — переконайтесь що встановлений Microsoft Word або вимкніть **Word COM** у налаштуваннях
- Якщо вікно не вміщується при масштабі — потягніть за край вікна щоб розтягнути

---

## Збірка .exe (для розробника)

Запустіть `build_exe.bat` — скрипт автоматично:
1. Створить Python-оточення та встановить залежності
2. Зберере один exe-файл у папку `dist\JapanMoto\`
3. Створить ярлик на робочому столі

> Для збірки потрібен Python 3.11+ та Internet для завантаження пакетів.
